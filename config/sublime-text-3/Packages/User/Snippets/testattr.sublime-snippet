<snippet>
    <content><![CDATA[
/** @test */
public function a_${1:model}_has_${2:attribute}_attribute()
{
    \$${1:model} = factory(${3:ModelName}::class)->make();

    \$this->assertEquals(__('${4:model.attribute}'), \$${1:model}->${2:attribute});
}
]]></content>
    <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
    <tabTrigger>testattr</tabTrigger>
    <!-- Optional: Set a scope to limit where the snippet will trigger -->
    <scope>source.php</scope>
</snippet>
